
if (caiu == 2) exit

//se a variavel colidiu não tiver um valor igual a 0
if (colidiu != 1){
	//quando for ativo
	//o vspeed vai ser igual a -4
	vspeed = global.varv;

	//define p começar a rodar a animação pelo quadro 1
	image_index = 1;
	//define a velocidade da animação p 1.5
	image_speed = 1.5;}